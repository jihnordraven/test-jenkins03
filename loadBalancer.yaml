apiVersion: v1
kind: Service
metadata:
  name: test-app-loadBalancer
  labels:
    app: test-app-loadBalancer
spec:
  selector:
    app: test-app-loadBalancer
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 4200